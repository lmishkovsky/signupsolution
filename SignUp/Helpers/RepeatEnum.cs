using System;

namespace SignUp.Helpers
{
    /// <summary>
    /// Defines how the signup event is repeated
    /// </summary>
    public enum RepeatEnum
    {
        Never,
        Daily,
        Weekly,
        Monthly,
        Yearly,
        Custom
    }
}
